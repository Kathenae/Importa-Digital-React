import Layout from "@/Layouts/Layout";
import { PageProps } from "@/types";

export default function Home({ }: PageProps) {
    return (
        <Layout>
            <h1>Home</h1>
        </Layout>
    )
}
